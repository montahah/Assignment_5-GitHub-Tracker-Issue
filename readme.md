1️⃣ What is the difference between var, let, and const?
Answer: Var has lot's of problem like assign multiple times, can call before initialize; let and var is Modern. let is for changing variable and const is for fixed.
2️⃣ What is the spread operator (...)?
Answer: Spread items from Array and Objects like individual items
3️⃣ What is the difference between map(), filter(), and forEach()?
Answer:map() is for transform is element into a new value;
filter() is for new array with only the elements that pass the test
forEach() is for perform action and returns undefined
4️⃣ What is an arrow function?
Answer: Arrow function introduced in es6 and it's modern  
5️⃣ What are template literals?
Answer: this is called backtick `` it is very powerful, it gives us for implement expressions
